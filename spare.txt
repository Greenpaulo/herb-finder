<AnimatedFlatList
  // scrollEventThrottle={16}
  // bounces={false}
  data={herbs}
  renderItem={({herb, index}) => (
    // <HerbCard herb={herb} y={y} index={index} />
    <Text>{herb[index]}</Text>
  )}
  // keyExtractor={ herb => herb.index}
  // {...{ onScroll }}
/>